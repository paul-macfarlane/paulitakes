---
name: verify
description: "Build/launch/drive recipe for runtime-verifying changes in this repo (Next.js dev server + local Docker Postgres)."
---

Read and follow `AGENTS.md`, then `docs/harness/workflows/verify.md` (paths relative to the repository root).

Use the arguments supplied in the user’s request, including native `$verify` invocation. The shared workflow is authoritative.
