---
name: backlog
description: "Show backlog status across epics and the next runnable task"
---

Read and follow `AGENTS.md`, then `docs/harness/workflows/backlog.md` (paths relative to the repository root).

Use the arguments supplied in the user’s request, including native `$backlog` invocation. The shared workflow is authoritative.
