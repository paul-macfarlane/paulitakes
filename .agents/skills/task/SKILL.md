---
name: task
description: "Run a backlog task through the implementation pipeline (clarify → execute → test & review → share)"
---

Read and follow `AGENTS.md`, then `docs/harness/workflows/task.md` (paths relative to the repository root).

Use the arguments supplied in the user’s request, including native `$task` invocation. The shared workflow is authoritative.
